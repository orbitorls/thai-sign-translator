[package]
name = "keypoint-extractor"
version = "0.1.0"
edition = "2021"

[dependencies]
native-windows-gui = { version = "1.0.13", default-features = false, features = ["file-dialog", "notice", "textbox"] }
